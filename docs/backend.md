# docs/backend.md

> Backend reference สำหรับ FastAPI + Python 3.12+
> Default stack: FastAPI / Pydantic v2 / SQLAlchemy 2.0 async / httpx

---

## 1. Setup

### 1.1 Python Version

ใช้ Python **3.12+** (scaffold + Dockerfile pin ไว้ที่ 3.12 — ต่ำกว่านี้จะ install fail)

### 1.2 Dependency Management

ใช้ **uv** หรือ **pip + pyproject.toml**:

**`backend/pyproject.toml`** (template — project name จะถูก set โดย `init_project.py`):

```toml
[project]
# name + description ถูกตั้งโดย scripts/init_project.py
name = "PROJECT_SLUG-backend"
version = "0.1.0"
description = "Project description"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.6.0",
    "pydantic-settings>=2.2.0",
    "sqlalchemy[asyncio]>=2.0.27",
    "asyncpg>=0.29.0",
    "alembic>=1.13.0",
    "httpx>=0.27.0",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "msal>=1.27.0",
    "anthropic>=0.25.0",
    "structlog>=24.1.0",
    "apscheduler>=3.10.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.3.0",
    "black>=24.2.0",
    "mypy>=1.8.0",
    "httpx>=0.27.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.black]
line-length = 100
target-version = ["py312"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

---

## 2. Project Structure

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── api/
│   │   ├── deps.py
│   │   ├── errors.py
│   │   └── v1/
│   │       ├── router.py
│   │       ├── users.py
│   │       └── products.py
│   ├── core/
│   │   ├── config.py           # Settings (pydantic-settings)
│   │   ├── security.py
│   │   └── logging.py
│   ├── db/
│   │   ├── session.py
│   │   ├── base.py
│   │   └── models/
│   ├── schemas/
│   ├── services/
│   ├── repositories/
│   └── integrations/
│       ├── azure_ad.py
│       └── claude_ai.py
├── alembic/
├── tests/
├── pyproject.toml
├── .env.example
└── Dockerfile
```

---

## 3. Configuration

**`app/core/config.py`:**

```python
from functools import lru_cache
from typing import Literal

from pydantic import Field, field_validator
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """Application settings — read from env vars.

    Note: scaffold/init_project ใช้ชื่อ env var แบบ canonical (ไม่มี prefix).
    `ENV_VAR_PREFIX` ใน project.config เป็น advisory เท่านั้น (ไม่ถูกเติมลง .env)
    เพราะ AUTH_SCOPE / AUTH_BOOTSTRAP_* อ่านผ่าน raw `os.getenv` ไม่ผ่าน Settings —
    ถ้าจะใช้ prefix จริงต้องตั้ง `env_prefix` ที่นี่ + แก้ os.getenv ทุกจุดเอง.
    """

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=True,
        extra="ignore",
        # env_prefix ไม่ถูกตั้งโดย scaffold — ดู note ด้านบนก่อน enable เอง
    )

    # App
    APP_NAME: str
    APP_ENV: Literal["dev", "staging", "production"] = "dev"
    APP_DEBUG: bool = False
    APP_LOG_LEVEL: Literal["DEBUG", "INFO", "WARNING", "ERROR"] = "INFO"

    # API
    API_V1_PREFIX: str = "/api/v1"
    API_CORS_ORIGINS: list[str] = Field(default_factory=list)

    # Database
    DB_HOST: str
    DB_PORT: int = 5432
    DB_NAME: str
    DB_USER: str
    DB_PASSWORD: str
    DB_POOL_SIZE: int = 10
    DB_MAX_OVERFLOW: int = 20

    # Auth — Azure AD (ใส่ default empty ถ้า AUTH_SCOPE=external_only)
    AZURE_AD_TENANT_ID: str = ""
    AZURE_AD_CLIENT_ID: str = ""
    AZURE_AD_CLIENT_SECRET: str = ""

    # Auth — Local (ใส่ default empty ถ้า AUTH_SCOPE=internal_only)
    JWT_SECRET_KEY: str = ""
    JWT_ALGORITHM: str = "HS256"
    JWT_ACCESS_TOKEN_EXPIRE_MINUTES: int = 60
    JWT_REFRESH_TOKEN_EXPIRE_DAYS: int = 7

    # AI
    CLAUDE_API_KEY: str
    CLAUDE_MODEL: str = "claude-sonnet-4-20250514"

    # Rate limiting
    RATE_LIMIT_PER_MINUTE: int = 60

    @field_validator("API_CORS_ORIGINS", mode="before")
    @classmethod
    def parse_cors(cls, v: str | list[str]) -> list[str]:
        if isinstance(v, str):
            return [o.strip() for o in v.split(",") if o.strip()]
        return v

    @property
    def database_url(self) -> str:
        return (
            f"postgresql+asyncpg://{self.DB_USER}:{self.DB_PASSWORD}"
            f"@{self.DB_HOST}:{self.DB_PORT}/{self.DB_NAME}"
        )


@lru_cache
def get_settings() -> Settings:
    return Settings()  # type: ignore[call-arg]
```

**`backend/.env.example`** (template; actual `.env` generated by `init_project.py`):

```bash
# Generated by init_project.py — see project.config for source values

# App
APP_NAME=
APP_ENV=dev
APP_DEBUG=false
APP_LOG_LEVEL=INFO

# API
API_CORS_ORIGINS=http://localhost:5173

# Database
DB_HOST=db
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Auth — Azure AD (skip if external_only)
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=

# Auth — Local (skip if internal_only)
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256

# AI
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-20250514
```

---

## 4. Application Entry

**`app/main.py`:**

```python
from contextlib import asynccontextmanager

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.api.errors import register_exception_handlers
from app.api.v1.router import api_router
from app.core.config import get_settings
from app.core.logging import setup_logging
from app.db.session import close_db, init_db


@asynccontextmanager
async def lifespan(app: FastAPI):
    settings = get_settings()
    setup_logging(settings.APP_LOG_LEVEL)
    await init_db()
    yield
    await close_db()


def create_app() -> FastAPI:
    settings = get_settings()

    app = FastAPI(
        title=settings.APP_NAME,
        debug=settings.APP_DEBUG,
        lifespan=lifespan,
        docs_url="/docs" if settings.APP_ENV != "production" else None,
        redoc_url="/redoc" if settings.APP_ENV != "production" else None,
    )

    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.API_CORS_ORIGINS,
        allow_credentials=True,
        allow_methods=["GET", "POST", "PUT", "PATCH", "DELETE"],
        allow_headers=["*"],
    )

    register_exception_handlers(app)

    app.include_router(api_router, prefix=settings.API_V1_PREFIX)

    @app.get("/health", tags=["health"])
    async def health() -> dict[str, str]:
        return {"status": "ok"}

    return app


app = create_app()
```

---

## 5. Dependency Injection

**`app/api/deps.py`:**

```python
from collections.abc import AsyncGenerator
from typing import Annotated

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config import Settings, get_settings
from app.core.security import decode_jwt
from app.db.session import get_db_session
from app.db.models.user import User
from app.repositories.user_repository import UserRepository


async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with get_db_session() as session:
        yield session


SettingsDep = Annotated[Settings, Depends(get_settings)]
DbDep = Annotated[AsyncSession, Depends(get_db)]


bearer_scheme = HTTPBearer(auto_error=False)


async def get_current_user(
    credentials: Annotated[HTTPAuthorizationCredentials | None, Depends(bearer_scheme)],
    db: DbDep,
    settings: SettingsDep,
) -> User:
    if credentials is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Missing authentication token",
        )

    try:
        payload = decode_jwt(credentials.credentials, settings.JWT_SECRET_KEY, settings.JWT_ALGORITHM)
        user_id = payload.get("sub")
        if not user_id:
            raise ValueError("Missing sub claim")
    except Exception as exc:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid token",
        ) from exc

    repo = UserRepository(db)
    user = await repo.get_by_id(user_id)
    if user is None or not user.is_active:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="User not found or inactive",
        )

    return user


CurrentUser = Annotated[User, Depends(get_current_user)]


def require_role(*allowed_roles: str):
    """Authorization dependency factory."""

    async def checker(user: CurrentUser) -> User:
        if not any(role in user.roles for role in allowed_roles):
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="Insufficient permissions",
            )
        return user

    return checker
```

---

## 6. Routes (Endpoint Patterns)

**`app/api/v1/products.py`:**

```python
from typing import Annotated

from fastapi import APIRouter, Depends, Query, status

from app.api.deps import CurrentUser, DbDep, require_role
from app.schemas.common import CursorPage
from app.schemas.product import ProductCreate, ProductRead, ProductUpdate
from app.services.product_service import ProductService

router = APIRouter(prefix="/products", tags=["products"])


@router.get("", response_model=CursorPage[ProductRead])
async def list_products(
    db: DbDep,
    user: CurrentUser,
    cursor: Annotated[str | None, Query(description="Pagination cursor")] = None,
    limit: Annotated[int, Query(ge=1, le=100)] = 20,
    status_filter: Annotated[str | None, Query(alias="status")] = None,
) -> CursorPage[ProductRead]:
    service = ProductService(db)
    return await service.list_products(
        cursor=cursor,
        limit=limit,
        status=status_filter,
        user=user,
    )


@router.get("/{product_id}", response_model=ProductRead)
async def get_product(
    product_id: str,
    db: DbDep,
    user: CurrentUser,
) -> ProductRead:
    service = ProductService(db)
    return await service.get_product(product_id, user=user)


@router.post(
    "",
    response_model=ProductRead,
    status_code=status.HTTP_201_CREATED,
    dependencies=[Depends(require_role("admin", "product_manager"))],
)
async def create_product(
    payload: ProductCreate,
    db: DbDep,
    user: CurrentUser,
) -> ProductRead:
    service = ProductService(db)
    return await service.create_product(payload, user=user)


@router.patch(
    "/{product_id}",
    response_model=ProductRead,
    dependencies=[Depends(require_role("admin", "product_manager"))],
)
async def update_product(
    product_id: str,
    payload: ProductUpdate,
    db: DbDep,
    user: CurrentUser,
) -> ProductRead:
    service = ProductService(db)
    return await service.update_product(product_id, payload, user=user)


@router.delete(
    "/{product_id}",
    status_code=status.HTTP_204_NO_CONTENT,
    dependencies=[Depends(require_role("admin"))],
)
async def delete_product(
    product_id: str,
    db: DbDep,
    user: CurrentUser,
) -> None:
    service = ProductService(db)
    await service.delete_product(product_id, user=user)
```

---

## 7. Schemas (Pydantic v2)

**`app/schemas/common.py`:**

```python
from typing import Generic, TypeVar

from pydantic import BaseModel, ConfigDict
from pydantic.alias_generators import to_camel

T = TypeVar("T")


class CamelBaseModel(BaseModel):
    """Base for API schemas: camelCase JSON, snake_case Python."""

    model_config = ConfigDict(
        alias_generator=to_camel,
        populate_by_name=True,
        from_attributes=True,
    )


class PaginationMeta(CamelBaseModel):
    next_cursor: str | None = None
    has_more: bool


class CursorPage(CamelBaseModel, Generic[T]):
    data: list[T]
    pagination: PaginationMeta


class ErrorDetail(CamelBaseModel):
    field: str
    code: str
    message: str


class ErrorResponse(CamelBaseModel):
    code: str
    message: str
    details: list[ErrorDetail] | None = None
    request_id: str
```

**`app/schemas/product.py`:**

```python
from datetime import datetime
from decimal import Decimal
from typing import Literal

from pydantic import Field

from app.schemas.common import CamelBaseModel


class ProductBase(CamelBaseModel):
    name: str = Field(min_length=1, max_length=200)
    sku: str = Field(pattern=r"^[A-Z0-9-]{3,32}$")
    business_unit_id: str
    category: str = Field(max_length=50)
    price: Decimal = Field(ge=0, decimal_places=2)
    status: Literal["draft", "active", "discontinued"]


class ProductCreate(ProductBase):
    pass


class ProductUpdate(CamelBaseModel):
    name: str | None = Field(default=None, min_length=1, max_length=200)
    category: str | None = Field(default=None, max_length=50)
    price: Decimal | None = Field(default=None, ge=0, decimal_places=2)
    status: Literal["draft", "active", "discontinued"] | None = None


class ProductRead(ProductBase):
    id: str
    created_at: datetime
    updated_at: datetime
```

---

## 8. Services (Business Logic)

Services เป็น **pure Python** ไม่ผูกกับ FastAPI — รับ session + payload เป็น argument

**`app/services/product_service.py`:**

```python
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models.user import User
from app.repositories.product_repository import ProductRepository
from app.schemas.common import CursorPage, PaginationMeta
from app.schemas.product import ProductCreate, ProductRead, ProductUpdate
from app.services.errors import NotFoundError, PermissionError


class ProductService:
    def __init__(self, db: AsyncSession) -> None:
        self.db = db
        self.repo = ProductRepository(db)

    async def list_products(
        self,
        *,
        cursor: str | None,
        limit: int,
        status: str | None,
        user: User,
    ) -> CursorPage[ProductRead]:
        items, next_cursor = await self.repo.list_paginated(
            cursor=cursor,
            limit=limit,
            status=status,
            business_unit_ids=user.business_unit_ids,
        )
        return CursorPage(
            data=[ProductRead.model_validate(p) for p in items],
            pagination=PaginationMeta(
                next_cursor=next_cursor,
                has_more=next_cursor is not None,
            ),
        )

    async def get_product(self, product_id: str, *, user: User) -> ProductRead:
        product = await self.repo.get_by_id(product_id)
        if product is None:
            raise NotFoundError(f"Product {product_id} not found")
        if product.business_unit_id not in user.business_unit_ids:
            raise PermissionError("No access to this business unit")
        return ProductRead.model_validate(product)

    async def create_product(self, payload: ProductCreate, *, user: User) -> ProductRead:
        if payload.business_unit_id not in user.business_unit_ids:
            raise PermissionError("No access to this business unit")
        product = await self.repo.create(payload.model_dump())
        await self.db.commit()
        return ProductRead.model_validate(product)

    async def update_product(
        self, product_id: str, payload: ProductUpdate, *, user: User
    ) -> ProductRead:
        product = await self.repo.get_by_id(product_id)
        if product is None:
            raise NotFoundError(f"Product {product_id} not found")
        if product.business_unit_id not in user.business_unit_ids:
            raise PermissionError("No access to this business unit")

        updates = payload.model_dump(exclude_unset=True)
        for key, value in updates.items():
            setattr(product, key, value)
        await self.db.commit()
        return ProductRead.model_validate(product)

    async def delete_product(self, product_id: str, *, user: User) -> None:
        product = await self.repo.get_by_id(product_id)
        if product is None:
            raise NotFoundError(f"Product {product_id} not found")
        if product.business_unit_id not in user.business_unit_ids:
            raise PermissionError("No access to this business unit")
        await self.repo.delete(product)
        await self.db.commit()
```

**`app/services/errors.py`:**

```python
class ServiceError(Exception):
    pass


class NotFoundError(ServiceError):
    pass


class PermissionError(ServiceError):
    pass


class ConflictError(ServiceError):
    pass


class ValidationError(ServiceError):
    pass
```

---

## 9. Error Handling

**`app/api/errors.py`:**

```python
import uuid

import structlog
from fastapi import FastAPI, Request, status
from fastapi.exceptions import RequestValidationError
from fastapi.responses import JSONResponse

from app.services.errors import (
    ConflictError,
    NotFoundError,
    PermissionError,
    ValidationError,
)

logger = structlog.get_logger()


def _error_response(
    status_code: int,
    code: str,
    message: str,
    request_id: str,
    details: list[dict] | None = None,
) -> JSONResponse:
    payload: dict = {
        "error": {
            "code": code,
            "message": message,
            "requestId": request_id,
        }
    }
    if details:
        payload["error"]["details"] = details
    return JSONResponse(status_code=status_code, content=payload)


def register_exception_handlers(app: FastAPI) -> None:
    @app.exception_handler(NotFoundError)
    async def not_found_handler(request: Request, exc: NotFoundError) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        return _error_response(
            status.HTTP_404_NOT_FOUND, "NOT_FOUND", str(exc), rid
        )

    @app.exception_handler(PermissionError)
    async def permission_handler(request: Request, exc: PermissionError) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        return _error_response(
            status.HTTP_403_FORBIDDEN, "FORBIDDEN", str(exc), rid
        )

    @app.exception_handler(ConflictError)
    async def conflict_handler(request: Request, exc: ConflictError) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        return _error_response(
            status.HTTP_409_CONFLICT, "CONFLICT", str(exc), rid
        )

    @app.exception_handler(ValidationError)
    async def validation_handler(request: Request, exc: ValidationError) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        return _error_response(
            status.HTTP_422_UNPROCESSABLE_ENTITY, "VALIDATION_ERROR", str(exc), rid
        )

    @app.exception_handler(RequestValidationError)
    async def pydantic_handler(request: Request, exc: RequestValidationError) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        details = [
            {
                "field": ".".join(str(p) for p in err["loc"]),
                "code": err["type"],
                "message": err["msg"],
            }
            for err in exc.errors()
        ]
        return _error_response(
            status.HTTP_422_UNPROCESSABLE_ENTITY,
            "VALIDATION_ERROR",
            "Request validation failed",
            rid,
            details,
        )

    @app.exception_handler(Exception)
    async def fallback_handler(request: Request, exc: Exception) -> JSONResponse:
        rid = request.headers.get("x-request-id", str(uuid.uuid4()))
        # NEVER leak internal details in production
        logger.exception("unhandled_exception", request_id=rid, path=request.url.path)
        return _error_response(
            status.HTTP_500_INTERNAL_SERVER_ERROR,
            "INTERNAL_ERROR",
            "An internal error occurred",
            rid,
        )
```

---

## 10. Async Patterns

### 10.1 Database calls

```python
# ✅ Correct
async def get_user(db: AsyncSession, user_id: str) -> User | None:
    result = await db.execute(select(User).where(User.id == user_id))
    return result.scalar_one_or_none()
```

### 10.2 External HTTP calls

```python
import httpx

async def fetch_external_data(url: str) -> dict:
    async with httpx.AsyncClient(timeout=10.0) as client:
        response = await client.get(url)
        response.raise_for_status()
        return response.json()
```

### 10.3 Parallel calls

```python
import asyncio

async def get_product_full(product_id: str, db: AsyncSession) -> dict:
    product, reviews, related = await asyncio.gather(
        repo.get_product(product_id),
        repo.get_reviews(product_id),
        repo.get_related_products(product_id),
    )
    return {"product": product, "reviews": reviews, "related": related}
```

### 10.4 Don't block the event loop

```python
import asyncio
from concurrent.futures import ThreadPoolExecutor

executor = ThreadPoolExecutor(max_workers=4)

async def run_sync_in_thread(func, *args):
    loop = asyncio.get_running_loop()
    return await loop.run_in_executor(executor, func, *args)
```

---

## 11. Background Tasks (APScheduler)

**`app/core/scheduler.py`:**

```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from apscheduler.triggers.cron import CronTrigger
from apscheduler.triggers.interval import IntervalTrigger

scheduler = AsyncIOScheduler()


def setup_jobs() -> None:
    from app.services.cleanup_service import cleanup_expired_sessions

    # Daily at 6:00 Bangkok (example — add your project's cron jobs here)
    # scheduler.add_job(
    #     your_daily_job_function,
    #     trigger=CronTrigger(hour=6, minute=0, timezone="Asia/Bangkok"),
    #     id="your_daily_job",
    #     replace_existing=True,
    # )

    # Hourly
    scheduler.add_job(
        cleanup_expired_sessions,
        trigger=IntervalTrigger(hours=1),
        id="cleanup_sessions",
        replace_existing=True,
    )
```

### 11.1 FastAPI BackgroundTasks (สำหรับ post-response work เล็กๆ)

```python
from fastapi import BackgroundTasks

@router.post("/products")
async def create_product(
    payload: ProductCreate,
    background_tasks: BackgroundTasks,
    db: DbDep,
):
    product = await service.create_product(payload)
    background_tasks.add_task(send_notification, product.id)
    return product
```

ใช้สำหรับงานสั้นๆ (< 1 วินาที) เท่านั้น

---

## 12. Logging (Structured)

**`app/core/logging.py`:**

```python
import logging
import sys

import structlog


def setup_logging(level: str = "INFO") -> None:
    timestamper = structlog.processors.TimeStamper(fmt="iso", utc=True)

    shared_processors = [
        structlog.contextvars.merge_contextvars,
        structlog.stdlib.add_log_level,
        structlog.stdlib.add_logger_name,
        timestamper,
        structlog.processors.StackInfoRenderer(),
        structlog.processors.format_exc_info,
    ]

    structlog.configure(
        processors=shared_processors + [structlog.processors.JSONRenderer()],
        wrapper_class=structlog.make_filtering_bound_logger(
            getattr(logging, level.upper())
        ),
        context_class=dict,
        logger_factory=structlog.PrintLoggerFactory(file=sys.stdout),
        cache_logger_on_first_use=True,
    )
```

ใช้งาน:

```python
import structlog
logger = structlog.get_logger()

async def create_product(payload: ProductCreate, *, user: User):
    logger.info(
        "product_create_started",
        user_id=str(user.id),
        sku=payload.sku,
        business_unit_id=payload.business_unit_id,
    )
```

**ห้าม log:** password, token, full credit card, full ID number, raw API key

---

## 13. Health Check

```python
@app.get("/health")
async def health() -> dict[str, str]:
    return {"status": "ok"}


@app.get("/health/ready")
async def readiness(db: DbDep) -> dict[str, str]:
    try:
        await db.execute(text("SELECT 1"))
    except Exception:
        raise HTTPException(status_code=503, detail="Database not ready")
    return {"status": "ready"}
```

---

## 14. Quick Reference: เมื่อ AI ได้รับ task

1. **Read first:** AGENTS.md (sections 2-7) + this file
2. **Create endpoint:**
   - Define schema in `schemas/`
   - Add repository method in `repositories/`
   - Add service method in `services/`
   - Add route in `api/v1/`
   - Register in `api/v1/router.py`
3. **Add migration:** `alembic revision --autogenerate -m "description"` then review/edit
4. **Update tests:** unit (service) + integration (route)
5. **Check handover docs:** update `onboarding.md` if env vars changed, `architecture.md` if module added
